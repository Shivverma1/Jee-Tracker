# Base URL of the FastAPI backend. Leave blank to use the Vite dev proxy (/api).
# For production (Vercel) set this to your Render backend URL, e.g.
# VITE_API_URL=https://jee-tracker-api.onrender.com
VITE_API_URL=
